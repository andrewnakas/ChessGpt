`sample.csv` is a stratified sample of the Lichess puzzle database
(https://database.lichess.org/#puzzles, CC0). It keeps about 150 puzzles for each tactical
theme the motif detectors cover, plus 400 random ones. `FEN` is the position before the
opponent's move; the first move in `Moves` is that opponent move, and the rest is the solution.
